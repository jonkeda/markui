interface:
  display_name: "MarkUI"
  short_description: "Generate, edit, and validate MarkUI wireframes"
  default_prompt: "Use $markui to create a clean MarkUI ASCII wireframe from my UI requirements."

policy:
  allow_implicit_invocation: true
