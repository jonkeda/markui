{
  "name": "@jonkeda/markui-validator",
  "version": "0.1.0",
  "description": "Shared MarkUI validation contract for CLI and MCP tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "pnpm run build && vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@jonkeda/markui-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  },
  "files": [
    "dist"
  ]
}
